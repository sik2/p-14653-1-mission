apiVersion: v1
kind: Service
metadata:
  name: nginx-nodeport
spec:
  type: NodePort           # ⭐ 외부 접근 허용
  selector:
    app: nginx
  ports:
  - port: 80               # 클러스터 내부 포트
    targetPort: 80         # Pod 포트
    nodePort: 30080        # ⭐ 외부에서 접근할 포트 (30000-32767)