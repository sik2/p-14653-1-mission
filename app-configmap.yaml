apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:                      # ⭐ 키-값 쌍으로 설정 저장
  APP_ENV: "development"
  APP_DEBUG: "true"
  DATABASE_HOST: "db-service"
  DATABASE_PORT: "5432"